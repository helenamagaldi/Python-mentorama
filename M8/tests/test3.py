import threading
import time

# threads name
threading.currentThread()

# how many threads are actually actrive
threading.active_count()

# shows a list with all active threads
threading.enumerate()

# number that identifies a thread
threading.get_ident()